# import pytest

from pprint import pprint as pp

from utils import get_posts_all, get_posts_by_user, get_comments_by_post_id, search_for_posts, get_post_by_pk
from config import PATH_POSTS, PATH_COMMENTS

pp(PATH_POSTS)



# pp(PATH_COMMENTS)

# pp(get_posts_all())
# pp(get_posts_by_user('leo'))
# pp(get_comments_by_post_id(4))
# pp(search_for_posts('вышел погулять'))
# pp(get_post_by_pk(4))
